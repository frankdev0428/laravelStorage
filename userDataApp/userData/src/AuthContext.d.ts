declare module './Context/AuthContext' {
    import { createContext } from 'react';
  
    const AuthContext: React.Context<any>; // Replace 'any' with the actual type if available
  
    export default AuthContext;
  }